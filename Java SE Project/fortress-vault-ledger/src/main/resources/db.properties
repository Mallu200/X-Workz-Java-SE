# JSE Topic: External Configuration (File I/O)
# This file is loaded at runtime by the DbConfig utility.

# Database Connection URL
db.url=jdbc:mysql://localhost:3306/fvl_db

# Database Credentials
db.user=root
db.pass=Mallu@2K3

# [SYSTEM NOTE]: Ensure the MySQL Connector JAR is in your classpath 
# to allow JDBC to interpret the 'jdbc:mysql' protocol.